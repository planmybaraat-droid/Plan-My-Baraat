'use client';
import { useCallback, useEffect, useState } from 'react';
import { AlertTriangle, Award, CheckCircle2, Clock3, FileCheck2, Loader2, ShieldCheck, TrendingUp } from 'lucide-react';
import CrmHeader from '../../crm/components/CrmHeader';
import { useSidebar } from '../../crm/sidebar-context';
import { loadPerformance, type PerformanceResult } from '../../crm/lib/performance-data';
const today=()=>new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Kolkata',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date());
const money=(v:number)=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(v);
export default function MyPerformancePage(){const {open}=useSidebar();const [month,setMonth]=useState(today().slice(0,7));const [result,setResult]=useState<PerformanceResult|null>(null);const [loading,setLoading]=useState(true);const [error,setError]=useState('');const load=useCallback(async()=>{setLoading(true);setError('');const start=`${month}-01`;const last=new Date(Number(month.slice(0,4)),Number(month.slice(5,7)),0).getDate();const end=`${month}-${String(last).padStart(2,'0')}`;try{const data=await loadPerformance({start,end,selfOnly:true});setResult(data.results[0]||null);}catch(c){setError(c instanceof Error?c.message:'Unable to load your performance.');}finally{setLoading(false);}},[month]);useEffect(()=>{load();},[load]);return <><CrmHeader title="My Performance" subtitle="Your live score and estimated monthly incentive" onMenuClick={open} notificationsHref="/workspace/notifications"/><main className="mx-auto max-w-6xl space-y-4 p-3 sm:p-6"><div className="flex justify-end"><input type="month" value={month} onChange={e=>setMonth(e.target.value)} className="h-10 rounded-xl border border-gray-200 bg-white px-3 text-xs font-bold outline-none focus:border-red-500"/></div>{error&&<p className="rounded-xl border border-red-200 bg-red-50 p-3 text-xs font-semibold text-red-700">{error}</p>}{loading?<div className="flex h-72 items-center justify-center"><Loader2 className="animate-spin text-red-600"/></div>:!result?<div className="rounded-2xl border border-gray-200 bg-white p-16 text-center text-sm text-gray-500">Your staff record is not available for this period.</div>:<><section className="overflow-hidden rounded-3xl bg-gradient-to-br from-gray-950 via-gray-900 to-red-950 p-5 text-white shadow-xl sm:p-8"><div className="grid gap-6 sm:grid-cols-2"><div><p className="text-[10px] font-black uppercase tracking-[.2em] text-red-400">Current performance</p><p className="mt-3 text-5xl font-black">{result.totalScore}<span className="text-xl text-gray-500"> / 100</span></p><p className="mt-3 max-w-md text-xs leading-relaxed text-gray-400">Calculated automatically from your attendance, completed hours, punctuality, breaks and submitted daily reports.</p></div><div className="sm:text-right"><p className="text-[10px] font-black uppercase tracking-[.2em] text-red-400">{result.status} incentive</p><p className="mt-3 text-4xl font-black">{money(result.incentiveAmount)}</p><p className="mt-3 text-xs text-gray-400">The amount remains estimated until management approves the period.</p></div></div></section><div className="grid grid-cols-2 gap-3 lg:grid-cols-5"><Metric icon={CheckCircle2} label="Attendance" value={result.attendanceScore} max={25} note={`${result.presentDays}/${result.eligibleDays} present`}/><Metric icon={Clock3} label="8 Working Hours" value={result.workingHoursScore} max={20} note={`${result.completedHoursDays} days completed`}/><Metric icon={TrendingUp} label="Timing" value={result.punctualityScore} max={20} note={`${result.lateDays} late arrivals`}/><Metric icon={ShieldCheck} label="Break Compliance" value={result.breakScore} max={20} note={`${result.breakViolationDays} violations`}/><Metric icon={FileCheck2} label="Daily Work Report" value={result.dailyReportScore} max={15} note={`${result.reportDays} submitted`}/></div><section className="rounded-2xl border border-gray-200 bg-white shadow-sm"><div className="border-b border-gray-100 p-4"><h2 className="text-sm font-black">Recent performance activity</h2><p className="mt-1 text-[10px] text-gray-400">The newest events affecting your live score.</p></div><div className="divide-y divide-gray-100">{result.days.filter(d=>d.state!=='Excluded').slice(-8).reverse().map(day=>{const warning=day.state==='Complete'&&(!day.completedHours||day.late||!day.breakCompliant||!day.reportSubmitted||day.attendance==='Absent');return <div key={day.date} className="flex items-start gap-3 p-4">{warning?<AlertTriangle size={17} className="mt-0.5 shrink-0 text-amber-500"/>:<CheckCircle2 size={17} className="mt-0.5 shrink-0 text-emerald-500"/>}<div className="min-w-0"><p className="text-xs font-bold">{new Date(`${day.date}T12:00:00`).toLocaleDateString('en-IN',{day:'numeric',month:'long',year:'numeric'})}</p><p className="mt-1 text-[10px] leading-relaxed text-gray-500">{day.reason}</p></div><span className="ml-auto shrink-0 rounded-full bg-gray-100 px-2 py-1 text-[9px] font-black uppercase text-gray-500">{day.state}</span></div>})}</div></section></>}</main></>}
function Metric({icon:Icon,label,value,max,note}:{icon:typeof Award;label:string;value:number;max:number;note:string}){return <div className="rounded-2xl border border-gray-200 bg-white p-4 shadow-sm"><span className="flex h-9 w-9 items-center justify-center rounded-xl bg-red-50 text-red-600"><Icon size={16}/></span><p className="mt-4 text-xl font-black">{value}<span className="text-xs text-gray-300">/{max}</span></p><p className="mt-1 text-[10px] font-black uppercase tracking-wider text-gray-500">{label}</p><p className="mt-2 text-[10px] text-gray-400">{note}</p></div>}
